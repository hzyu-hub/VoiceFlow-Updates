# VoiceFlow Updates

Public update feed and DMG assets for VoiceFlow.

This repository hosts anonymous update metadata (`update.json`) and release DMG files so the VoiceFlow app can auto-update while the source repository remains private.